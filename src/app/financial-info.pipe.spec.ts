import { FinancialInfoPipe } from './financial-info.pipe';

describe('FinancialInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new FinancialInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
